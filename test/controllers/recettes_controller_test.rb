require 'test_helper'

class RecettesControllerTest < ActionDispatch::IntegrationTest
  describe RecettesController do
    # let(:user1) { users(:one) }

    it "can get index" do
      # VCR.use_cassette("index_action") do
      #   # get root_path
      #   # must_respond_with :success
      # end
    end
    it "can get show" do
      # VCR.use_cassette("index_action") do
      #   # get recettes_path("Apple Chips")
      #   # must_respond_with :success
      # end
    end
  end

end
